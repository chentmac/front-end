server:
  port: 9967

spring:
  application:
    name: EUREKA

security:
  basic:
    enabled: true
  user:
    name: admin
    password: 123456

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${security.user.name}:${security.user.password}@localhost:9966/eureka/  # ????????
